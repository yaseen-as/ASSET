# Copy to .env and fill in for local training runs.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=insights_db
DB_USER=admin
DB_PASSWORD=admin123

# Universe + label config
UNIVERSE=top_100_nse
LABEL_HORIZON_DAYS=5
LABEL_THRESHOLD=0.03

# Output
ARTIFACT_DIR=./artifacts
