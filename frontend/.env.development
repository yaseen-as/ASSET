# VITE_API_BASE_URL is intentionally unset here.
# - In k8s dev:  ingress routes /api/* → api-gateway  (relative /api/v1 works)
# - Local dev:   Vite proxy routes /api/* → localhost:3000  (relative /api/v1 works)
# Only set this if you need to point at a remote API server.
