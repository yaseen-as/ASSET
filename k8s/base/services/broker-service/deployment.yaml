apiVersion: apps/v1
kind: Deployment
metadata:
  name: broker-service
  labels:
    app: broker-service
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: broker-service
  template:
    metadata:
      labels:
        app: broker-service
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: app
          image: yaseenas/broker-service:latest
          ports:
            - containerPort: 3003
          workingDir: /app/services/broker-service
          envFrom:
            - configMapRef:
                name: broker-service-config
            - secretRef:
                name: broker-service-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 15
