apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-initdb
  namespace: database
data:
  # Grants CREATEDB to the admin user so each service's ensureDatabase()
  # can create its own database (core_db, insights_db).
  grant-createdb.sql: |
    ALTER USER admin CREATEDB;
