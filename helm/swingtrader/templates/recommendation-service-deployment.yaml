apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  labels:
    app: recommendation-service
    app.kubernetes.io/component: backend
    environment: {{ .Values.global.environment }}
spec:
  replicas: {{ .Values.recommendationService.replicas }}
  selector:
    matchLabels:
      app: recommendation-service
  template:
    metadata:
      labels:
        app: recommendation-service
        app.kubernetes.io/component: backend
    spec:
      initContainers:
        {{- include "swingtrader.waitForPostgres" . | nindent 8 }}
      containers:
        - name: app
          {{- if .Values.hotReload.enabled }}
          image: {{ .Values.hotReload.nodeImage }}
          command: ["sh", "-c"]
          args:
            - "rm -f /app/packages/shared/tsconfig.tsbuildinfo && (cd /app && npm run build --workspace=packages/shared) && npm run dev"
          {{- else }}
          image: {{ .Values.recommendationService.image.repository }}:{{ .Values.recommendationService.image.tag }}
          {{- end }}
          workingDir: {{ .Values.recommendationService.workingDir }}
          ports:
            - containerPort: {{ .Values.recommendationService.port }}
              name: http
          envFrom:
            - configMapRef:
                name: recommendation-service-config
          {{- if .Values.hotReload.enabled }}
          env:
            - name: NODE_ENV
              value: development
          volumeMounts:
            {{- include "swingtrader.hotReloadVolumeMount" . | nindent 12 }}
          {{- end }}
          {{- if .Values.recommendationService.probes.enabled }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.recommendationService.port }}
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.recommendationService.port }}
            initialDelaySeconds: 45
            periodSeconds: 15
          {{- end }}
          {{- with .Values.recommendationService.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if .Values.hotReload.enabled }}
      volumes:
        {{- include "swingtrader.hotReloadVolume" . | nindent 8 }}
      {{- end }}
