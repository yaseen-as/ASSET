apiVersion: apps/v1
kind: Deployment
metadata:
  name: feature-service
  labels:
    app: feature-service
    app.kubernetes.io/component: backend
    environment: {{ .Values.global.environment }}
spec:
  replicas: {{ .Values.featureService.replicas }}
  selector:
    matchLabels:
      app: feature-service
  template:
    metadata:
      labels:
        app: feature-service
        app.kubernetes.io/component: backend
    spec:
      initContainers:
        {{- include "swingtrader.waitForPostgres" . | nindent 8 }}
      containers:
        - name: app
          {{- if .Values.hotReload.enabled }}
          image: {{ .Values.hotReload.nodeImage }}
          command: ["sh", "-c"]
          args:
            - "rm -f /app/packages/shared/tsconfig.tsbuildinfo && (cd /app && npm run build --workspace=packages/shared) && npm run dev"
          {{- else }}
          image: {{ .Values.featureService.image.repository }}:{{ .Values.featureService.image.tag }}
          {{- end }}
          workingDir: {{ .Values.featureService.workingDir }}
          ports:
            - containerPort: {{ .Values.featureService.port }}
              name: http
          envFrom:
            - configMapRef:
                name: feature-service-config
          {{- if .Values.hotReload.enabled }}
          env:
            - name: NODE_ENV
              value: development
          volumeMounts:
            {{- include "swingtrader.hotReloadVolumeMount" . | nindent 12 }}
          {{- end }}
          {{- if .Values.featureService.probes.enabled }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.featureService.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.featureService.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
          {{- end }}
          {{- with .Values.featureService.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if .Values.hotReload.enabled }}
      volumes:
        {{- include "swingtrader.hotReloadVolume" . | nindent 8 }}
      {{- end }}
