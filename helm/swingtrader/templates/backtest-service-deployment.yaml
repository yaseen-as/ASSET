apiVersion: apps/v1
kind: Deployment
metadata:
  name: backtest-service
  labels:
    app: backtest-service
    app.kubernetes.io/component: backend
    environment: {{ .Values.global.environment }}
spec:
  replicas: {{ .Values.backtestService.replicas }}
  selector:
    matchLabels:
      app: backtest-service
  template:
    metadata:
      labels:
        app: backtest-service
        app.kubernetes.io/component: backend
    spec:
      initContainers:
        {{- include "swingtrader.waitForPostgres" . | nindent 8 }}
      containers:
        - name: app
          {{- if .Values.hotReload.enabled }}
          image: {{ .Values.hotReload.nodeImage }}
          command: ["sh", "-c"]
          args:
            - "rm -f /app/packages/shared/tsconfig.tsbuildinfo && (cd /app && npm run build --workspace=packages/shared) && npm run dev"
          {{- else }}
          image: {{ .Values.backtestService.image.repository }}:{{ .Values.backtestService.image.tag }}
          {{- end }}
          workingDir: {{ .Values.backtestService.workingDir }}
          ports:
            - containerPort: {{ .Values.backtestService.port }}
              name: http
          envFrom:
            - configMapRef:
                name: backtest-service-config
          {{- if .Values.hotReload.enabled }}
          env:
            - name: NODE_ENV
              value: development
          volumeMounts:
            {{- include "swingtrader.hotReloadVolumeMount" . | nindent 12 }}
          {{- end }}
          {{- if .Values.backtestService.probes.enabled }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.backtestService.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.backtestService.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
          {{- end }}
          {{- with .Values.backtestService.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if .Values.hotReload.enabled }}
      volumes:
        {{- include "swingtrader.hotReloadVolume" . | nindent 8 }}
      {{- end }}
