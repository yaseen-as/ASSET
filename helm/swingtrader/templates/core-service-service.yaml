apiVersion: v1
kind: Service
metadata:
  name: core-service
  labels:
    app: core-service
spec:
  selector:
    app: core-service
  ports:
    - port: 80
      targetPort: {{ .Values.coreService.port }}
