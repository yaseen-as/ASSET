apiVersion: v1
kind: Service
metadata:
  name: feature-service
  labels:
    app: feature-service
spec:
  selector:
    app: feature-service
  ports:
    - port: 80
      targetPort: {{ .Values.featureService.port }}
